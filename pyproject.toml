[tool.poetry]
name = "zapdocuments"
version = "0.1.0"
description = ""
authors = ["Thiago Beviláqua"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
django = "^5.1.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
